#include <stdio.h>
#include <string.h>

/**
 * _strlen - reset the value of the passed variable
 *
 * @s: the string variable
 *
 * Return: Returns string length
 */

int _strlen(char *s)
{
	return (strlen(s));
}
